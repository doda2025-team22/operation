apiVersion: v1
kind: Pod
metadata:
  name: {{ include "team22_chart.fullname" . }}-test-connection
  labels:
    {{- include "team22_chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "team22_chart.fullname" . }}:{{ .Values.modelservice.port }}']
  restartPolicy: Never
