# Create a Service for the fronend
#! Still needs to communicate with the backend service apparently. Try to find the docs for it.
apiVersion: v1
kind: Service
metadata:
    name: app-svc
spec:
    type: ClusterIP
    selector:
        app: app-service
    ports:
        -   port: {{ .Values.app.stable.port }} # Exposed Port
            targetPort: {{ .Values.app.stable.port }} # forward port

---
# Create a Service for the backend
apiVersion: v1
kind: Service
metadata:
    name: model-svc
spec:
    type: ClusterIP
    selector:
        app: model-service
    ports:
        -   port: {{ .Values.modelservice.stable.port }} # Exposed Port
            targetPort: {{ .Values.modelservice.stable.port }} # forward port