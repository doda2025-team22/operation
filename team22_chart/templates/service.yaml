apiVersion: v1
kind: Service
metadata:
  name: {{ include "team22_chart.fullname" . }}
  labels:
    {{- include "team22_chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "team22_chart.selectorLabels" . | nindent 4 }}
